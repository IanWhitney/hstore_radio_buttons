require_relative '../../test_helper.rb'

describe HstoreRadioButtons::Configuration do
    before :each do
      @it = HstoreRadioButtons::Configuration.new
    end

    #describe "#build_for_model" do
      #it "returns just the butt
    #end
end


    #config_file = File.open('./test/support/config/hstore_radio_button_sets.yml')
    #config = YAML.load(config_file)
    #button_sets = config["person"].keys
